function [k, mu_array] = DPmeans(dataMatrix, lambda, mu)

% Initialize k and mu_array
k = 1;
mu_array = mu;

% Initialze all labels of data matrix to 1
labels = ones(length(dataMatrix), 1);

% Find distance for each point from given center mu
distanceFrom_mu = euclideanDistance(dataMatrix, mu);

% Find all the distances that are more than the current center's distance away





end

